import { FiltroUsuariosPipe } from './filtro-usuarios.pipe';

describe('FiltroUsuariosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroUsuariosPipe();
    expect(pipe).toBeTruthy();
  });
});
